"""Smart Room adapter placeholder.

This module will load one Smart Room camera/sensor recording and normalize it
into a GroundingEval scene representation.

Planned responsibilities:
- Use metadata.json when available.
- Discover camera streams.
- Discover timestamps.
- Discover calibration data.
- Discover scripted events.
- Return a normalized scene representation for scenario generation.

Implementation is intentionally deferred.
"""
